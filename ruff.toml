target-version = "py312"
line-length = 88

[lint]
# Conservative set: real errors (pyflakes), style (pycodestyle), import order.
select = ["E", "F", "I"]

[lint.isort]
force-sort-within-sections = true
known-first-party = ["custom_components.nextenergy"]
